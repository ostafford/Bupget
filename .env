# Application Settings
# This controls which configuration to use (development, testing, or production)
# For local development, keep this as "development"
FLASK_ENV=development

# This secret key is used for securely signing session cookies and other security features
# In production, this should be a long, random string that's kept secret
# For development, any string will work, but don't use this value in production
SECRET_KEY=your-secret-key-here

# Database Connection
# This tells the application how to connect to your database
# Format: dialect://username:password@host:port/database_name
# Example for SQLite (simple file-based database, good for development):
# DATABASE_URI=sqlite:///dev.db
# Example for PostgreSQL with Docker:
DATABASE_URI=postgresql://postgres:password@db/budget_app
# Example for PostgreSQL on localhost:
# DATABASE_URI=postgresql://postgres:password@localhost/budget_app

# Up Bank API
# You'll need to generate a Personal Access Token from the Up Bank developer portal
# Visit: https://developer.up.com.au/ to create your token
UP_BANK_API_TOKEN=your-up-bank-personal-access-token